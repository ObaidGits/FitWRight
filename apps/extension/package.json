{
  "name": "fitwright-extension",
  "version": "0.1.0",
  "private": true,
  "description": "FitWright Companion - capture jobs, autofill applications, and score matches from your own browser.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../fitwright-extension.zip . -x '*.map'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "0.0.287",
    "esbuild": "0.24.0",
    "jsdom": "28.1.0",
    "typescript": "5.6.3",
    "vitest": "4.1.8"
  }
}
